name: axum-zero2prod

region: sgp1

services:
  - name: axum-zero2prod
    dockerfile_path: Dockerfile
    source_dir: .
    health_check:
      http_path: /health_check
    github:
      branch: main
      deploy_on_push: false
      repo: lednhatkhanh/axum-zero2prod
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
